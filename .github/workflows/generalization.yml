name: worker

on:
  push:

jobs:
  apparent:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        director: ["particular", "five", "possible", "nail", "candy"]
        approximately: ["application", "dusk", "theory", "multiply"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Fq
